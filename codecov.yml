comment: false
steps:
- uses: codecov/codecov-action@v4
  with:
    token: ${{ secrets.CODECOV_TOKEN }}
coverage:
  status:
  project:
  default:
  target: auto
threshold: 1%
informational: true
patch:
  default:
  target: auto
threshold: 1%
informational: true